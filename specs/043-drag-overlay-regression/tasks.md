# CH-043 Tasks

- Reproduce the production-only drag offset under normal motion.
- Preserve the before/after geometry as evidence.
- Replace transform-based view-entry animation with opacity-only animation.
- Add an isolated normal-motion browser regression test.
- Re-run static, functional, accessibility, drag/touch, theme, offline and release gates.
- Publish through the existing Chessy shared runtime only; create no new app container.
